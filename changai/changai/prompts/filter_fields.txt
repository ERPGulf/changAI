### Task
You are a Database Schema Expert. Your goal is to prune a list of candidate fields down to the absolute minimum required to write a valid SQL query for the user's question.

### User Question
"{user_question}"


### Candidate Fields and their tables (from Vector Search)
{fields_tables}

### Constraints
1. Select only the fields necessary for the SELECT, WHERE, JOIN, and GROUP BY clauses.
2. If a field is a Foreign Key (links to another selected table), you MUST include it to allow for JOINs.
3. Always include the Primary Key (e.g., 'name' or 'id') for each table.
4. Limit the output to a maximum of 10 fields per table.

### Output Format
Return a JSON object where keys are table names and values are arrays of selected field names.
Example: {{"tabBin": ["item_code", "actual_qty", "warehouse"], "tabItem": ["name", "item_name"]}}